# written by /plugin-types inside a plugin folder; version-specific, never committed
.claude/types/
node_modules/
.DS_Store
# built by docs/build.mjs and published by the pages workflow; never committed
site/
.playwright-mcp/
