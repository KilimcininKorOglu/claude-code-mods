{
  "description": "sql-concat-watch: session.start, command.run, tool.call on Edit and Write. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.ts"]
}
