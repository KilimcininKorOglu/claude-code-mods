{
  "description": "contract-watch: session.start, command.run{command=contract-watch}, tool.call{tool=Edit}. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.ts"]
}
