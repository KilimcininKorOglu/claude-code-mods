{
  "description": "storage-guard: session.start, command.run, turn.complete, prompt.submit, tool.call on Bash, Edit and Write. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.ts"]
}
