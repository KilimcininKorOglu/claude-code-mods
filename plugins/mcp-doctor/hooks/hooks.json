{
  "description": "mcp-doctor: session.start, command.run, turn.complete, prompt.attachment. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.ts"]
}
