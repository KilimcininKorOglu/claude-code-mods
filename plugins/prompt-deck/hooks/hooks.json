{
  "description": "prompt-deck: session.start, command.run, prompt.submit, ui.render on AbovePrompt. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.tsx"]
}
