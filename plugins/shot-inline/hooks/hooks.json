{
  "description": "shot-inline: session.start, command.run, tool.call on Read, Bash and the Playwright screenshot, ui.render on ToolUse. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.tsx"]
}
