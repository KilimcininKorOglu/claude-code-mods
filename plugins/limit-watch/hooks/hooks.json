{
  "description": "limit-watch: session.start, turn.complete, command.run{command=limits}, ui.render{component=Pane}. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": [
    "./register.tsx"
  ]
}
