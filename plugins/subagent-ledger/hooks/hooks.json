{
  "description": "subagent-ledger: session.start, command.run{command=subagent-ledger}, agent.spawn, turn.step, turn.complete. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.ts"]
}
