{
  "description": "bg-tasks: session.start, command.run, tool.call on Bash and TaskStop, prompt.submit from task notifications, ui.render on Pane. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.tsx"]
}
